# 🎮 Sea Salt & Paper - 啟動指南

歡迎使用 Sea Salt & Paper 線上卡牌遊戲！本指南將幫助您快速開始開發和測試。

## 📋 目錄

- [環境需求](#環境需求)
- [快速開始](#快速開始)
- [開發模式](#開發模式)
- [AI 測試與優化](#ai-測試與優化)
- [測試](#測試)
- [部署](#部署)
- [專案結構](#專案結構)
- [常見問題](#常見問題)

## 環境需求

- **Node.js**: 18.0+ (推薦 LTS 版本)
- **npm**: 9.0+
- **Firebase 帳號**: 用於多人遊戲和資料同步

## 快速開始

### 1. 安裝依賴

```bash
npm install
```

### 2. 設定環境變數

複製環境變數範本：

```bash
cp .env.example .env.local
```

編輯 `.env.local` 並填入你的 Firebase 配置：

```env
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain
VITE_FIREBASE_DATABASE_URL=your_database_url
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_storage_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

### 3. 啟動開發伺服器

```bash
npm run dev
```

開啟瀏覽器訪問：http://localhost:5173

## 開發模式

### 本機開發

```bash
# 啟動開發伺服器（支援熱重載）
npm run dev

# 在另一個終端機運行測試（watch 模式）
npm run test
```

### 程式碼檢查

```bash
# ESLint 檢查
npm run lint
```

### 建置專案

```bash
# 建置生產版本
npm run build

# 預覽生產版本
npm run preview
```

## AI 測試與優化

### 🤖 批次對戰系統

本專案包含完整的 AI 批次對戰系統，用於測試和優化 AI 策略。

#### 快速測試（30 場）

適合快速驗證 AI 邏輯：

```bash
npm run battle:quick
```

#### 標準測試（300 場）

提供可靠的統計數據：

```bash
npm run battle:full
```

#### 大規模測試（1000 場）

用於深度分析和參數優化：

```bash
npm run battle:extensive
```

#### 自定義配置

```bash
# 自定義遊戲數量
npm run battle -- --games 500

# 只測試特定策略
npm run battle -- --strategies medium,hard

# 指定輸出目錄
npm run battle -- --output ./my-results
```

### 📊 查看測試結果

測試完成後，結果會保存在 `scripts/output/` 目錄：

```
scripts/output/
├── battle_stats_[timestamp].json    # 詳細統計數據
├── battle_report_[timestamp].txt    # 可讀報告
└── EXAMPLE_OUTPUT.md                # 輸出範例說明
```

### 🔧 AI 參數調整

根據測試結果，你可以調整 AI 參數來優化策略：

編輯 `src/services/aiService.js`：

```javascript
// 困難 AI 的宣告閾值
const declareThreshold = 7  // 調整此值（建議範圍：5-11）

// 回合數閾值
const turnThreshold = 10    // 調整此值（建議範圍：5-20）

// 牌堆基礎價值
const deckBaseValue = 3     // 調整此值（建議範圍：2-5）
```

修改後重新運行批次對戰驗證效果：

```bash
npm run battle:full
```

**詳細說明**：查看 [scripts/README.md](./scripts/README.md) 和 [AI_OPTIMIZATION_PLAN.md](./AI_OPTIMIZATION_PLAN.md)

## 測試

### 單元測試

```bash
# 運行所有單元測試
npm test

# UI 模式（互動式測試）
npm run test:ui

# 測試覆蓋率報告
npm run test:coverage
```

### E2E 測試（Playwright）

```bash
# 運行 E2E 測試
npm run test:e2e

# UI 模式（可視化測試過程）
npm run test:e2e:ui

# 除錯模式
npm run test:e2e:debug

# 查看測試報告
npm run test:e2e:report
```

## 部署

### Firebase Hosting

1. **安裝 Firebase CLI**

```bash
npm install -g firebase-tools
```

2. **登入 Firebase**

```bash
firebase login
```

3. **初始化專案（首次）**

```bash
firebase init hosting
```

選擇：
- Public directory: `dist`
- Configure as SPA: `Yes`
- Set up automatic builds: `No`

4. **建置並部署**

```bash
# 建置專案
npm run build

# 部署到 Firebase
firebase deploy --only hosting
```

### 部署檢查清單

- [ ] 環境變數已正確設定
- [ ] Firebase 規則已更新
- [ ] 所有測試通過
- [ ] 建置成功無錯誤
- [ ] 在本機預覽版本正常運作

## 專案結構

```
board-game-sea-salt-paper/
├── src/
│   ├── components/          # React 組件
│   │   ├── common/          # 可重用組件（Button, Card, Modal）
│   │   ├── game/            # 遊戲組件（PlayerHand, ScorePanel）
│   │   └── pages/           # 頁面組件（HomePage, GameBoard）
│   ├── services/            # 業務邏輯
│   │   ├── gameService.js   # 遊戲核心邏輯
│   │   ├── aiService.js     # AI 決策邏輯
│   │   └── firebaseService.js
│   ├── data/                # 遊戲資料
│   │   ├── cards.js         # 72 張卡牌定義
│   │   └── cardDescriptions.js
│   ├── utils/               # 工具函數
│   ├── hooks/               # 自定義 React Hooks
│   └── config/              # 配置文件
├── scripts/                 # 批次對戰系統
│   ├── batchBattle.js       # 主程式
│   ├── utils/               # 工具類
│   ├── output/              # 測試結果輸出
│   └── README.md            # 批次對戰說明
├── tests/                   # 測試文件
├── e2e/                     # E2E 測試
└── public/                  # 靜態資源
```

## 常見問題

### Q: Firebase 連接失敗？

**A:** 檢查以下項目：
1. `.env.local` 文件是否存在且配置正確
2. Firebase 專案是否啟用 Realtime Database
3. 網路連接是否正常

### Q: AI 對戰不運作？

**A:**
1. 確認已正確匯入 AI 服務
2. 檢查控制台是否有錯誤訊息
3. 運行單元測試驗證 AI 邏輯：`npm test`

### Q: 批次對戰運行很慢？

**A:**
- 批次對戰是 CPU 密集型操作
- 建議先用 `battle:quick` 驗證邏輯
- 正式測試使用 `battle:full` 即可
- 只在需要深度分析時使用 `battle:extensive`

### Q: 如何調試遊戲邏輯？

**A:**
1. 使用瀏覽器開發者工具的 Console
2. 在關鍵函數添加 `console.log`
3. 使用 React DevTools 檢查組件狀態
4. 運行單元測試隔離問題：`npm test`

### Q: 建置失敗怎麼辦？

**A:**
```bash
# 清除快取
rm -rf node_modules/.vite

# 重新安裝依賴
npm install

# 重新建置
npm run build
```

### Q: 如何貢獻程式碼？

**A:**
1. Fork 專案
2. 建立功能分支：`git checkout -b feature/new-feature`
3. 提交修改：`git commit -m "feat: add new feature"`
4. 推送分支：`git push origin feature/new-feature`
5. 建立 Pull Request

## 開發工作流程建議

### 新功能開發

1. **規劃** - 更新 TASK.md
2. **實作** - 編寫程式碼和單元測試
3. **測試** - 運行所有測試確保通過
4. **AI 驗證** - 如涉及遊戲邏輯，運行批次對戰
5. **審查** - 檢查程式碼品質
6. **提交** - 使用語義化提交訊息

### Bug 修復

1. **重現** - 建立測試案例重現 bug
2. **修復** - 修改程式碼
3. **驗證** - 確保測試通過
4. **回歸測試** - 運行完整測試套件
5. **提交** - 描述修復內容

## 相關文件

- [專案規劃](./PLANNING.md) - 完整架構和開發計劃
- [任務追蹤](./TASK.md) - 詳細任務分解
- [資料庫設計](./DATABASE_DESIGN.md) - Firebase 結構
- [設計規範](./DESIGN_SPEC.md) - UI/UX 設計系統
- [AI 優化計劃](./AI_OPTIMIZATION_PLAN.md) - AI 改進策略
- [批次對戰說明](./scripts/README.md) - 詳細使用指南
- [Claude 開發指南](./CLAUDE.md) - AI 輔助開發規範

## 技術支援

如需協助：
1. 查看相關文件
2. 檢查 Issues
3. 查看控制台錯誤訊息
4. 運行測試診斷問題

---

**版本**: 1.0.0
**最後更新**: 2025-01-14
**專案狀態**: 開發中 → AI 優化階段

🎮 **祝開發順利！**
